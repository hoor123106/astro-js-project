---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import '../../styles/notes-index.css';

const notes = await getCollection('notes');
---

<Layout title="Fragrance Notes Directory">
	<header>
        <h1>Fragrance <span class="text-gradient">Notes</span> Directory</h1>
        <p>A dynamically generated encyclopedia of fragrance notes fetched directly from Google Sheets.</p>
    </header>
    
    <div class="grid">
        {notes.map((note) => {
            const slug = note.id.replace(/\.(md|mdx)$/, '');
    return (
   <a href={`/notes/${slug}`} class="card">
            <h2>{note.data.title}</h2>
            <span class="group-badge">{note.data.group}</span>
            <p>{note.data.odorProfile?.substring(0, 120)}...</p>
        </a>
    );
})}
    </div>
</Layout>

